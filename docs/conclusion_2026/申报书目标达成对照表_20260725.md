# 申报书目标达成对照表（结项版，2026-07-25）

> 说明：本表用于把原申报目标转换为结项答辩口径。结论分为“已完成”“阶段完成/需继续累积”“待人工闭环”。财务部分不在本表中填数，由负责人按学校财务系统核对。

| 申报书目标/验收关注点 | 当前完成情况 | 主要证据 | 结项结论 | 风险口径 |
|---|---|---|---|---|
| 建设 3000+ 实验室安全知识库 | 已形成 `3009` 条结构化知识片段，29 列统一字段，覆盖危化品、设备、生物、电气、消防、PPE、应急等主题。 | `knowledge_base_curated.csv`；`libs/kb_schema.py`；`scripts/quality_gate.py`；Dify 导入报告。 | 已完成 | 表述为“结构化知识片段”，不是 3009 份独立文档。 |
| 来源可追溯 | `source_title/source_org/source_url/references` 字段缺失为 0；已审计 `716` 个唯一 URL。 | `artifacts/kb_traceability_20260725/kb_traceability_summary.json`；`kb_source_url_audit.csv`。 | 已完成基础闭环 | 当前自动化实测：352 open、333 blocked_or_forbidden、31 network_error；不能说全部可打开。 |
| PDF 和本地证据留存 | `173` 个 URL 有本地原件或官方镜像证据；`151` 个 PDF 来源中 `111` 个已有本地 PDF 原件（80 个本地 PDF 文件）。 | `docs/eval/source_backup_coverage_20260725.md`；`docs/eval/pdf_backup_completion_round_20260725.md`；`artifacts/source_backup_coverage_20260725/source_backup_coverage.csv`。 | 阶段完成/需继续补档 | 仍缺 `40` 个 PDF 原件（站点 CDN/WAF 拦截或已改为 HTML 网页，非死链，原因逐条记录）；镜像/Markdown 不等同于原始 PDF。 |
| 基于 Dify + RAG 的问答链路 | 已实现 FastAPI 代理、Dify 上游 SSE、本地检索和结构化降级；历史在线验证显示 Dify 可返回结构化回答。 | `README.md`；`HANDOVER.md`；`docs/conclusion_2026/Dify回退根因排查_超时与并发配置_20260723.md`；代码与运行记录。 | 已完成原型链路 | 7/25 本机体验复测未配置 `DIFY_APP_API_KEY`，只验证规则 + 本地检索兜底；送审前建议补 Dify SSE 截图。 |
| 高风险规则拦截与应急回答 | `safety_rules.yaml` 维护 29 条规则；本轮修复泄漏/中毒、爆炸事故、激光、电击、冻伤等应急模板与匹配优先级。 | `safety_rules.yaml`；`web_demo/services/answer_service.py`；`web_demo/services/kb_service.py`；`tests/test_emergency_rules.py`。 | 已完成并补强 | 新增高风险规则时仍需同步检查硬编码模板。 |
| 有效回答率 >95% | 自动回归可响应；本地规则与快答链路已通过现有测试。 | `eval_set_v2_50.csv`；`docs/eval/eval_50_human_scored.md`；本轮 pytest 结果。 | 待人工闭环 | 有效回答率应由独立专家签字评分确认，不能由关键词命中率替代。 |
| 专业准确率高目标 | 项目已通过多轮错误修复和回归补强；当前更适合表述为“重点场景可演示、可复核、可整改”。 | 历次 `docs/eval/` 报告；`docs/eval/expert_review_and_ux_improvement_20260725.md`。 | 阶段完成/持续优化 | 不承诺整体 3009 条 99% 准确率；以分层专家评审为准。 |
| 响应时间 <3 秒 | 本地规则/检索快答可毫秒级；Dify SSE 首事件历史样本可在 3 秒内出现，但完整回答受 LLM 影响更长。 | 运行监测、Dify 排查记录、本地浏览器复测。 | 阶段完成 | 申报指标建议解释为“首字节/首事件 <3s”；不把完整回答全部写成 <3s。 |
| Web Demo 友好界面 | 已有登录、对话、引用、反馈、状态栏、知识库态势；本轮新增移动端导航、引用来源域名/PDF 标签、评分文案修正、会话存储优化。 | `web_demo/frontend/src/components/MobileNavigation.tsx`；`CitationCard.tsx`；`ScoreBar.tsx`；`AuthContext.tsx`。 | 已完成并优化 | 当前仍是演示账号，不是正式统一身份认证。 |
| 7×24 与三个月试运行 | 运行快照已真实累积，远程主监测环境已接入，报告骨架已形成。 | `artifacts/runtime/health_snapshots.csv`；`artifacts/runtime/reports/runtime_latest_summary.md`；`docs/conclusion_2026/7x24每日阶段证据总览_20260723.md`。 | 阶段完成/需时间闭环 | 自 2026-07-01 起真实累积，完整三个月最早 2026-10-01 形成。 |
| 数据更新与维护机制 | 已有采集规范、清洗脚本、质量门禁、Dify 导入包、来源审计和整改计划。 | `DATA_COLLECTION_SPEC.md`；`DATA_RECOLLECTION_PLAN.md`；`AUDIT_GUIDE.md`；`scripts/quality_gate.py`。 | 已完成基础流程 | 后续重点是 PDF 补档、站点阻断人工复核和 reviewer/status 闭环。 |
| 安全可靠性 | 已有 Dify host 白名单、App Key 服务端保存、禁止透传入站 Authorization、SSE 连接关闭、降级回答和演示认证。 | `web_demo` 后端代码；`AGENTS.md` 致命 Bug 清单；测试。 | 已完成原型级安全加固 | 本地 HTTP Demo 不写“端到端加密”；正式部署需 HTTPS/统一认证/日志脱敏。 |

## 结项判断

按申报书目标和当前结项验收关注点拆分，本项目可归纳为 12 项技术与验收条件。截至 2026-07-25，当前结论为：**7 项已满足或基本满足，4 项阶段满足并需继续补充证据，1 项待专家人工复核闭环**。

| 状态分类 | 数量 | 对应项目 |
|---|---:|---|
| 已满足 / 基本满足 | 7 项 | 3000+知识库、来源可追溯基础、Dify+RAG原型链路、高风险规则拦截、Web Demo、数据更新维护机制、安全可靠性 |
| 阶段满足 / 需继续补证据 | 4 项 | PDF和本地证据留存、专业准确率高目标、响应时间<3秒、7×24与三个月试运行 |
| 待人工闭环 | 1 项 | 有效回答率>95%，需专家或安全管理人员按统一量表签字评分 |

项目已经完成申报书中“构建实验室安全问答原型、形成 3000+ 结构化知识库、实现 Dify/RAG 主链路、提供来源引用、规则兜底、演示页面和测试材料”的核心要求；结项前仍需要人工和行政流程闭环：财务核对、专家签字评分、负责人签字盖章，以及三个月运行周期届满后的完整证据导出。详细口径见 `docs/conclusion_2026/结项证据口径说明_12项条件_20260725.md`。
