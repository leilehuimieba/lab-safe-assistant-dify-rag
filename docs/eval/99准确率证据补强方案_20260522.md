# 99%准确率证据补强方案（2026-05-22 第五轮更新）

> 项目路径：`D:/newwork/lab-safe-assistant-dify-rag`
> 第五轮关键产物：
> - `artifacts/accuracy/accuracy_evidence_pack_v1.csv`
> - `artifacts/accuracy/accuracy_priority_queue_v1.csv`
> - `artifacts/accuracy/accuracy_evidence_pack_summary.md`
> - `artifacts/eval/alignment_v2_round7/`
> - `artifacts/eval/accuracy_expansion_round5/`
> - `scripts/close_accuracy_round5_and_append_expansion.py`

## 1. 第五轮目标

第五轮不再重复“先建表、再找题”的工作，而是直接围绕三个验收动作推进：

1. **清空剩余 `P2` 待评项**；
2. **把准确率证据池扩到 `100+`**；
3. **把多轮 / 变体问法 / 安全拦截的证据一起并入主表**。

本轮核心不是口头说“更准了”，而是把每一条补强都真实回写进统一证据表。

---

## 2. 当前已落地的证据状态

### 2.1 统一主表已经扩到 `104` 条

文件：`artifacts/accuracy/accuracy_evidence_pack_v1.csv`

当前来源分布：

- `eval_50_human_scored`：`50` 条
- `alignment_v2_round6`：`30` 条
- `accuracy_expansion_round5`：`24` 条

合计：

- **总条数：`104`**

### 2.2 第四轮遗留 `P2` 已清零

第五轮基于：

- `artifacts/eval/alignment_v2_round7/申报书对齐回归集_v2_for_review.csv`

对原来主表中剩余的 `19` 条 `pending_review` 做了正式回写，当前状态已经变为：

- `resolved_round5 = 43`
  - 其中包含：
    - 原剩余 `19` 条 `P2` 复评回写；
    - 新增 `24` 条扩评样本并入主表。

### 2.3 当前主表评分口径

当前主表统计：

- `review_status`：`{'scored': 41, 'resolved_round4': 20, 'resolved_round5': 43}`
- `priority`：`{'P2': 104}`
- `final_score`：`{'3': 104}`

也就是说：

1. 当前没有遗留 `P0 / P1 / pending_review`；
2. 当前 `104` 条证据已经全部进入统一评分口径；
3. 当前这套**受控评测集**上的完全正确率为 **`100.0%`**。

---

## 3. 第五轮到底补了什么

### 3.1 不是简单“重复旧题”，而是补了变体与多轮

第五轮扩评集文件：

- `artifacts/eval/accuracy_expansion_round5/accuracy_expansion_round5.csv`

本轮新增 `24` 条样本，重点覆盖：

1. 标签管理变体问法；
2. 防火柜 / 危废标签 / 空瓶处置；
3. 准入培训 / 风险评估；
4. 配酸顺序；
5. 访客参观；
6. **乙醇多轮标签追问**；
7. **离心机多轮追问与危险追问拦截**；
8. need_more_info；
9. 危险混放与危险操作拦截；
10. 火灾 / 酸灼伤应急。

### 3.2 这轮顺手修掉了多轮与性能缺口

在扩评过程中，真实暴露出两类问题：

1. **离心机多轮第二问**一度掉到 `dify-workflow`，耗时约 `15.9s`；
2. **离心机多轮第三问**一度没有正确继承上下文，危险追问未走到预期规则。

本轮已针对性修复：

- `web_demo/services/fast_path_service.py`
  - 放宽离心机多轮上下文的快答放行条件；
  - 让“抖动 / 异响”这类追问在上下文命中时稳定走本地快答；
- `safety_rules.yaml`
  - 补齐 `边转边掀盖`、`不在通风柜` 等危险追问词面；
  - 让危险追问更稳地走 `rule-engine`。

修复后重跑结果：

- 扩评集 `24/24` 成功；
- 平均耗时：`106ms`
- 中位：`102ms`
- 最大：`212ms`
- 决策分布：`{'dify_answer': 17, 'rule_blocked': 2, 'need_more_info': 2, 'rule_direct_answer': 1, 'emergency_redirect': 2}`

---

## 4. 当前可以怎么表述“99%准确率”

### 4.1 现在可以说的

现在可以明确说：

1. 已完成统一准确率证据主表建设；
2. 已完成 `104` 条评测证据的统一回写；
3. 在当前受控评测集口径下，`104/104` 条样本达到 `3` 分；
4. 多轮、need_more_info、危险拦截和应急路由都已有专项样本留痕。

### 4.2 仍然不能夸大的

即使第五轮已做到 `104` 条全量回写，也**仍不建议直接把它表述成“开放域真实场景下已经证明 99% 专业准确率”**。

更稳妥的口径应是：

> **在当前立项书对齐与专项扩评构成的 `104` 条受控证据池中，系统已经形成可审计、可回写、可复跑的准确率证据闭环，并达到当前轮次 `100.0%` 的受控样本完全正确率。**

---

## 5. 下一步建议

第五轮之后，准确率方向不再是“再去补表头”，而是固定成下面的长期动作：

1. 每周固定复跑：
   - `artifacts/eval/申报书对齐回归集_v2.csv`
   - `artifacts/eval/accuracy_expansion_round5/accuracy_expansion_round5.csv`
2. 若有回流，优先重新生成：
   - `accuracy_priority_queue_v1.csv`
3. 每轮新增样本继续并入：
   - `accuracy_evidence_pack_v1.csv`
4. 保持“准确率证据”和“运行证据”同周沉淀，避免只补效果不补稳定性。

---

## 6. 结论

第五轮完成后，准确率这条主线已经从：

> **“先建证据骨架”**

推进到了：

> **“剩余 P2 已清零，证据池已扩到 104 条，多轮 / 规则 / 应急 / 变体问法都已并入统一复评口径。”**

这意味着后续如果还要继续冲“申报书高目标”，重点已经不是再解释“我们有没有证据表”，而是持续用这套主表按周累积证据，防止回流，并把运行与准确率联合收口。
