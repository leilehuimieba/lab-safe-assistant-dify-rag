# 99%准确率证据补强方案（2026-05-22 第四轮更新）

> 项目路径：`D:/newwork/lab-safe-assistant-dify-rag`
> 本轮新增骨架：
> - `scripts/build_accuracy_evidence_pack.py`
> - `artifacts/accuracy/accuracy_evidence_pack_v1.csv`
> - `artifacts/accuracy/accuracy_priority_queue_v1.csv`
> - `artifacts/accuracy/accuracy_evidence_pack_summary.md`

## 1. 当前问题不是“没有准确率数据”，而是“没有足够支撑 99% 的证据包”

当前已有：

1. `50` 题人工评分；
2. 有效回答率 `100%`；
3. 完全正确率 `82%`；
4. 新版 30 题申报书对齐回归终态已稳定通过。

但距离立项书里的：

> **专业准确率 >99%**

还差两层证据：

1. **扩样**：不能只靠 50 题；
2. **复评**：要有专家或更严格人工复评的留痕。

因此，这一轮的目标不是虚报 `99%`，而是建立一套：

> **可以持续把准确率证据越攒越厚的评审骨架。**

---

## 2. 已落地的证据包结构

### 2.1 合并证据主表

文件：`artifacts/accuracy/accuracy_evidence_pack_v1.csv`

已合并两类来源：

1. 历史 `50` 题人工评分结果；
2. 最新 `30` 题申报书对齐回归结果。

当前规模：

- 总条数：`80`
- 已有人评：`50`
- 待补评：`30`

### 2.2 优先复评队列

文件：`artifacts/accuracy/accuracy_priority_queue_v1.csv`

当前自动筛出的优先队列：

- `P0`：`9` 条
  - 历史人工评分为 `2` 分的题，代表当前最明确的失分点；
- `P1`：`11` 条
  - 高风险题、规则链题、多轮关键题；
- 合计优先补评：`20` 条。

### 2.3 汇总说明

文件：`artifacts/accuracy/accuracy_evidence_pack_summary.md`

当前摘要：

- 80 条证据池已形成；
- 50 条已有评分基线；
- 完全正确率基线仍是 `82.0%`；
- 已经知道接下来先补哪些题，而不是再盲目扩题。

### 2.4 第四轮清队结果

第四轮已经把前面这份高优先级队列真正跑完了一轮复评回写：

- `artifacts/accuracy/accuracy_priority_queue_round4_recheck.csv`
  - 保存了 `20` 条 `P0/P1` 题目的第四轮复测结果；
- `artifacts/accuracy/accuracy_evidence_pack_v1.csv`
  - 已回写 `expert_score / expert_notes / final_score / final_notes`；
- `artifacts/accuracy/accuracy_priority_queue_v1.csv`
  - 当前已经清空为 `0` 条未解决 `P0/P1`；
- `artifacts/accuracy/accuracy_evidence_pack_summary.md`
  - 已更新为第四轮口径。

当前第四轮结论：

1. 高优先级 `P0/P1` 队列已清空；
2. 剩余待补评样本主要是 `P2` 与新增扩样题；
3. 准确率主线已从“堵最明显缺口”转为“继续扩样，补最终 99% 证据厚度”。

---

## 3. 这份证据骨架能解决什么

### 3.1 解决“扩样没抓手”

现在不需要从零再设计一份新表，直接在 `accuracy_evidence_pack_v1.csv` 上继续追加评分即可。

### 3.2 解决“哪些题先补不清楚”

现在优先顺序已经固定：

1. 先补 `P0` 的 9 条 2 分题；
2. 再补 `P1` 的 11 条关键安全题；
3. 然后继续向 `P2` 扩样。

### 3.3 解决“评分留痕不统一”

表里已经留好字段：

- `human_score`
- `human_notes`
- `expert_score`
- `expert_notes`
- `final_score`
- `final_notes`

后续无论是你自己打分、老师复评，还是专家复核，都能回填到同一张证据表里。

---

## 4. 下一步推荐补证路径

### 第一阶段：保持 `P0/P1 = 0`，避免回流

目标：

- 新增改动后优先做小回归，防止已清空的高优先级问题回流；
- 如有新失分题，优先进入新的 `P0/P1` 队列而不是混在 `P2` 中。

### 第二阶段：继续补 `P2` 与新增样本

目标：

- 在当前 `80` 条证据池基础上继续把 `P2` 与新增样本补到 `100+`；
- 形成专项准确率子报告，例如：
  - 高风险题准确率
  - 设备题准确率
  - 多轮题准确率

### 第三阶段：把证据池扩到 100+ 条

建议：

- 在现有 80 条基础上再扩 `20—40` 条；
- 按类别均衡补：化学 / 设备 / 应急 / 危废 / 生物 / 准入。

---

## 5. 当前能说到什么程度

### 可以说

- 已经完成准确率证据池与复评队列骨架建设；
- 已经有 50 条真实人工评分基线；
- 已经形成 80 条合并证据池，可继续扩样与专家复评。

### 不能说

- 已经达到 `99%` 专业准确率；
- 已经完成足够规模的专家复评；
- 已经形成最终准确率终验结论。

---

## 6. 结论

第四轮之后，准确率方向已经从“建立队列骨架”推进到：

> **高优先级 P0/P1 队列已清空，统一证据表、复评回写和后续扩样闭环都已经跑通。**

这意味着后面每一轮优化，都可以继续真实累积到“99% 准确率证据”这条主线上，而不会再散掉。
