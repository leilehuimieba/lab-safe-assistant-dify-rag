# 最小测试报告模板：基于 Dify 的实验室安全小助手

> 报告日期：2026-04-28  
> 项目路径：`D:\newwork\lab-safe-assistant-dify-rag`  
> 测试对象：基于 Dify 的实验室安全小助手最小问答系统  
> 报告状态：模板，待真实 Dify 配置与测试后填写  

## 1. 测试目的

本测试用于验证第一个项目是否具备申报书所需的最小可演示能力，包括：

1. 系统可启动；
2. Web 页面可访问；
3. Dify 配置状态可读取；
4. 本地知识库可检索；
5. 实验室安全问答接口可返回结果；
6. 高风险问题可触发安全规则；
7. Dify 不可用时可结构化兜底；
8. 测试结果可作为申报书和结题材料的证据。

---

## 2. 测试环境

| 项目 | 内容 |
|---|---|
| 测试日期 | 2026-04-28 |
| 操作系统 | Windows |
| 项目路径 | `D:\newwork\lab-safe-assistant-dify-rag` |
| Python 版本 | 待填写 |
| Dify Base URL | 待填写，例如 `http://127.0.0.1:8081` |
| Dify App Key | 已配置 / 未配置 |
| 本地演示端口 | `8088` |
| 知识库文件 | `knowledge_base_curated.csv` |
| Dify 导入包 | `release_exports/v8.2/knowledge_base_import_ready.csv` |
| 安全规则文件 | `safety_rules.yaml` |
| 评测集文件 | `eval_set_v1.csv` |

---

## 3. 测试前检查

### 3.1 依赖安装

执行：

```powershell
pip install -r requirements.txt
```

结果：

```text
待填写：成功 / 失败
```

### 3.2 质量门禁

执行：

```powershell
python scripts/quality_gate.py --repo-root . --skip-secret-scan
```

期望：

```text
Quality gate passed.
```

实际结果：

```text
待填写
```

### 3.3 环境变量

检查 `.env.dify_rag`：

| 字段 | 是否已配置 | 备注 |
|---|---|---|
| `DIFY_BASE_URL` | 待填写 |  |
| `DIFY_APP_API_KEY` | 待填写 | 不记录明文 Key |
| `DIFY_TIMEOUT` | 待填写 |  |
| `DEMO_PORT` | 待填写 |  |
| `ENABLE_EMBEDDING` | 待填写 |  |

---

## 4. 启动测试

执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/start_dify_rag_local.ps1
```

期望输出：

```text
[OK] Dify RAG app started
URL       : http://127.0.0.1:8088
Health    : http://127.0.0.1:8088/health
```

实际结果：

```text
待填写
```

运行证据文件：

| 文件 | 是否生成 |
|---|---|
| `artifacts/local-dify-rag/runtime.json` | 待填写 |
| `logs/dify_rag.log` | 待填写 |
| `logs/dify_rag.err.log` | 待填写 |

---

## 5. 接口测试

### 5.1 健康检查

命令：

```powershell
Invoke-RestMethod "http://127.0.0.1:8088/health"
```

期望：

```json
{"status":"ok"}
```

实际：

```text
待填写
```

结论：通过 / 不通过

---

### 5.2 元信息接口

命令：

```powershell
Invoke-RestMethod "http://127.0.0.1:8088/api/meta"
```

期望重点字段：

| 字段 | 期望 |
|---|---|
| `app_version` | `dify-rag-project-1` |
| `dify_base_url` | Dify API 地址 |
| `dify_app_key_configured` | `true` 为真实 Dify 链路；`false` 为兜底模式 |
| `knowledge_base_rows` | 大于 0 |
| `knowledge_base_imported` | 当前导入口径 |

实际：

```json
待填写
```

结论：通过 / 不通过

---

### 5.3 本地知识库检索

命令：

```powershell
Invoke-RestMethod "http://127.0.0.1:8088/api/search?q=化学品泄漏&top_k=5"
```

期望：

- `count > 0`；
- `citations` 不为空；
- 返回条目包含 `kb_id`、`title`、`snippet`、`score`。

实际：

```text
待填写
```

结论：通过 / 不通过

---

### 5.4 常规问答

命令：

```powershell
Invoke-RestMethod `
  -Uri "http://127.0.0.1:8088/api/chat" `
  -Method Post `
  -ContentType "application/json" `
  -Body '{"question":"使用强酸时需要注意什么？"}'
```

期望：

- 返回 `answer`；
- 返回 `citations`；
- 若 Dify 已配置，`model` 应尽量为 `dify-workflow`；
- 回答应包含 PPE、通风、禁止事项或应急提示。

实际：

```text
待填写
```

结论：通过 / 不通过

---

### 5.5 应急问题测试

命令：

```powershell
Invoke-RestMethod `
  -Uri "http://127.0.0.1:8088/api/chat" `
  -Method Post `
  -ContentType "application/json" `
  -Body '{"question":"实验室发生化学品泄漏时，第一步应该怎么做？"}'
```

期望：

- 命中安全规则或返回 Dify 应急回答；
- `decision` 可为 `emergency_redirect`、`dify_answer_guarded` 或类似安全决策；
- 回答包含隔离现场、避免接触、通知负责人、启动应急预案等内容。

实际：

```text
待填写
```

结论：通过 / 不通过

---

### 5.6 禁止操作测试

命令：

```powershell
Invoke-RestMethod `
  -Uri "http://127.0.0.1:8088/api/chat" `
  -Method Post `
  -ContentType "application/json" `
  -Body '{"question":"实验废液可以直接倒入下水道吗？"}'
```

期望：

- 返回禁止或保守提示；
- 不输出危险操作建议；
- 说明应按废液分类和本单位 SOP 处理。

实际：

```text
待填写
```

结论：通过 / 不通过

---

## 6. 20 题最小评测记录

> 从 `eval_set_v1.csv` 中选择 20 条代表性问题，覆盖危化品、设备、PPE、废弃物、应急处置和违规操作。

| 序号 | 问题 | 类型 | 是否成功响应 | 是否有引用 | 是否安全合规 | 人工判定 | 备注 |
|---:|---|---|---|---|---|---|---|
| 1 | 待填写 | 常规问答 |  |  |  |  |  |
| 2 | 待填写 | 常规问答 |  |  |  |  |  |
| 3 | 待填写 | 危化品 |  |  |  |  |  |
| 4 | 待填写 | 危化品 |  |  |  |  |  |
| 5 | 待填写 | PPE |  |  |  |  |  |
| 6 | 待填写 | PPE |  |  |  |  |  |
| 7 | 待填写 | 设备操作 |  |  |  |  |  |
| 8 | 待填写 | 设备操作 |  |  |  |  |  |
| 9 | 待填写 | 废弃物 |  |  |  |  |  |
| 10 | 待填写 | 废弃物 |  |  |  |  |  |
| 11 | 待填写 | 应急 |  |  |  |  |  |
| 12 | 待填写 | 应急 |  |  |  |  |  |
| 13 | 待填写 | 违规操作 |  |  |  |  |  |
| 14 | 待填写 | 违规操作 |  |  |  |  |  |
| 15 | 待填写 | 低置信 |  |  |  |  |  |
| 16 | 待填写 | 低置信 |  |  |  |  |  |
| 17 | 待填写 | 综合 |  |  |  |  |  |
| 18 | 待填写 | 综合 |  |  |  |  |  |
| 19 | 待填写 | 综合 |  |  |  |  |  |
| 20 | 待填写 | 综合 |  |  |  |  |  |

---

## 7. 指标汇总

| 指标 | 结果 | 说明 |
|---|---:|---|
| 测试问题数 | 待填写 | 建议 20 |
| 成功响应数 | 待填写 | 返回 HTTP 200 且有 answer |
| 成功响应率 | 待填写 | 成功响应数 / 测试问题数 |
| 有引用问题数 | 待填写 | citations 非空 |
| 引用返回率 | 待填写 | 有引用问题数 / 测试问题数 |
| 高风险问题数 | 待填写 | 应急/违规类问题 |
| 高风险合规响应数 | 待填写 | 拒答、应急引导或保守提示 |
| 高风险合规响应率 | 待填写 | 合规响应数 / 高风险问题数 |
| Dify 成功调用数 | 待填写 | model 为 `dify-workflow` |
| 结构化兜底数 | 待填写 | model 为 `fallback-rule-engine` |
| 平均响应时间 | 待填写 | 可选 |
| P95 响应时间 | 待填写 | 可选 |

---

## 8. 测试结论

### 8.1 已通过项

- 待填写

### 8.2 未通过项

- 待填写

### 8.3 主要问题

- 待填写

### 8.4 整改建议

- 待填写

---

## 9. 附录：推荐 smoke 命令

```powershell
cd D:\newwork\lab-safe-assistant-dify-rag

python scripts/quality_gate.py --repo-root . --skip-secret-scan

powershell -ExecutionPolicy Bypass -File scripts/start_dify_rag_local.ps1

Invoke-RestMethod "http://127.0.0.1:8088/health"
Invoke-RestMethod "http://127.0.0.1:8088/api/meta"
Invoke-RestMethod "http://127.0.0.1:8088/api/search?q=化学品泄漏&top_k=5"

Invoke-RestMethod `
  -Uri "http://127.0.0.1:8088/api/chat" `
  -Method Post `
  -ContentType "application/json" `
  -Body '{"question":"实验室发生化学品泄漏时，第一步应该怎么做？"}'

powershell -ExecutionPolicy Bypass -File scripts/status_dify_rag_local.ps1
powershell -ExecutionPolicy Bypass -File scripts/stop_dify_rag_local.ps1
```

---

## 10. 附录：当前已知基线

截至模板生成时，项目已知基线为：

| 项目 | 当前值 |
|---|---:|
| 本地知识库条目 | 142 |
| Dify 导入包条目 | 398 |
| 评测集条目 | 110 |
| 最小 quality gate | 已通过 |
| Dify Key | 待实际配置 |
| Dify 导入报告 | 待生成 |
| Dify smoke 报告 | 待填写 |
