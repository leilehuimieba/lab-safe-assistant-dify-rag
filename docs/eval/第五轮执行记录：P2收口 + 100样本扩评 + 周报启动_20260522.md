# 第五轮执行记录：P2收口 + 100样本扩评 + 周报启动（2026-05-22）

> 仓库：`D:/newwork/lab-safe-assistant-dify-rag`
> 分支：`codex/proposal-alignment-round2-20260522`
> 本轮目标：继续补 `P2`、把准确率证据扩到 `100+`、开始按周沉淀运行证据。

## 1. 本轮完成结果

本轮已完成三件核心事情：

1. **剩余 `19` 条 `P2` 全部复评回写完成**；
2. **新增 `24` 条扩评样本，准确率证据池扩到 `104` 条**；
3. **运行证据已产出首份 `2026W21` 周报和周问题记录**。

---

## 2. 准确率主线执行情况

### 2.1 处理前状态

处理前主表：

- `artifacts/accuracy/accuracy_evidence_pack_v1.csv`

当时状态：

- 总条数：`80`
- `review_status`：
  - `scored = 41`
  - `resolved_round4 = 20`
  - `pending_review = 19`

### 2.2 本轮执行动作

#### 动作 A：补强本地快答与规则命中

修改文件：

- `web_demo/services/fast_path_service.py`
- `safety_rules.yaml`

本轮补的重点不是新功能，而是针对评测题真实暴露出的缺口补词面和上下文：

1. 防火柜 / 空试剂瓶 / 封闭鞋 / 通风柜 / 危废标签等 P2 问法；
2. 离心机多轮第二问“抖动怎么办”的上下文快答；
3. 离心机多轮第三问“边转边掀盖”的危险追问拦截；
4. “不在通风柜里配”的危险词面。

#### 动作 B：重跑申报书 round7

评测目录：

- `artifacts/eval/alignment_v2_round7/`

结果：

- `30/30` 成功
- 平均耗时：`77ms`
- 中位：`73ms`
- 最大：`148ms`

#### 动作 C：新增扩评集并实跑

新增评测集：

- `artifacts/eval/accuracy_expansion_round5/accuracy_expansion_round5.csv`

扩评结果目录：

- `artifacts/eval/accuracy_expansion_round5/`

最终结果：

- `24/24` 成功
- 平均耗时：`106ms`
- 中位：`102ms`
- 最大：`212ms`

关键决策分布：

- `dify_answer = 17`
- `rule_blocked = 2`
- `need_more_info = 2`
- `rule_direct_answer = 1`
- `emergency_redirect = 2`

#### 动作 D：统一回写准确率主表

新增脚本：

- `scripts/close_accuracy_round5_and_append_expansion.py`

作用：

1. 把 round7 中剩余 `19` 条 `pending_review` 正式回写；
2. 把 `24` 条扩评样本并入 `accuracy_evidence_pack_v1.csv`；
3. 更新 `accuracy_priority_queue_v1.csv`；
4. 更新 `accuracy_evidence_pack_summary.md`。

### 2.3 处理后状态

当前主表：

- `artifacts/accuracy/accuracy_evidence_pack_v1.csv`

处理后统计：

- 总条数：`104`
- `review_status`：
  - `scored = 41`
  - `resolved_round4 = 20`
  - `resolved_round5 = 43`
- `final_score = {'3': 104}`
- `accuracy_priority_queue_v1.csv` 当前为空

这意味着：

1. 当前不再存在遗留 `pending_review`；
2. 当前不再存在未清空的高优先级复评队列；
3. 当前受控证据池已经达到 `100+` 且全部留痕。

---

## 3. 运行证据主线执行情况

### 3.1 本轮新增能力

修改 / 新增文件：

- `scripts/build_runtime_report.py`
- `scripts/build_weekly_runtime_report.ps1`

本轮新增能力：

1. `build_runtime_report.py` 支持：
   - `--iso-week`
   - `--date-from`
   - `--date-to`
   - `--last-days`
   - `--notes-output`
2. 可以直接生成周报与周问题记录。

### 3.2 本轮实际执行

已执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\run_runtime_check_once.ps1 -BaseUrl http://127.0.0.1:8091 -ReportLabel latest
powershell -ExecutionPolicy Bypass -File .\scripts\build_weekly_runtime_report.ps1 -BaseUrl http://127.0.0.1:8091 -IsoWeek 2026W21
```

### 3.3 当前运行产物

新增 / 更新：

- `artifacts/runtime/health_snapshots.jsonl`
- `artifacts/runtime/health_snapshots.csv`
- `artifacts/runtime/reports/runtime_latest_summary.md`
- `artifacts/runtime/reports/runtime_latest_summary.csv`
- `artifacts/runtime/reports/runtime_2026W21_summary.md`
- `artifacts/runtime/reports/runtime_2026W21_summary.csv`
- `artifacts/runtime/notes/weekly_issues_2026W21.md`

当前运行快照总数：

- `5`

本周摘要：

- `/health` 正常率：`100.0%`
- Dify 可达率：`100.0%`

---

## 4. 本轮验证结论

### 4.1 准确率

- P2 已清零；
- 证据池已扩到 `104`；
- 当前受控评测样本全部完成统一回写。

### 4.2 多轮 / need_more_info / 性能

本轮真实修掉的问题：

1. 离心机多轮第二问不再掉慢链路；
2. 离心机多轮危险追问可正确走 `rule-engine`；
3. “不在通风柜里配”这类危险问法可正确走拦截。

### 4.3 长期运行

- 当前还不能宣称“已经完成 3 个月试运行”；
- 但已经从“只有日检脚本”升级为“日检 + 周报 + 周问题记录”都能真实产出。

---

## 5. 下一步建议

下一步不建议再大改系统，而是按固定节奏持续沉淀：

1. 每周复跑：
   - `申报书对齐回归集_v2.csv`
   - `accuracy_expansion_round5.csv`
2. 每周执行：
   - `build_weekly_runtime_report.ps1`
3. 每周把异常 / 修复 / 版本变更补进：
   - `weekly_issues_<ISO周>.md`
4. 每月整理一次试运行小结。

---

## 6. 结论

第五轮已经把这条主线从：

> **“还剩一些 P2 待补、还没有 100+ 样本、周报还只是想法”**

推进到：

> **“P2 已清零、100+ 样本证据已成形、周报已开始真实产出。”**

这轮之后，项目离“版本4高目标收口”差的已经主要不是功能，而是持续执行与时间跨度。
