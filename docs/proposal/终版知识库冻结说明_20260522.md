# 终版知识库冻结说明（2026-05-22）

> 仓库：`D:/newwork/lab-safe-assistant-dify-rag`
> 当前主表：`D:/newwork/lab-safe-assistant-dify-rag/knowledge_base_curated.csv`
> 冻结日期：`2026-05-22`

## 1. 本次冻结的目标

本次不再扩充知识条目数量，而是做两件事：

1. **补齐当前主表中的关键缺失字段**；
2. **明确当前“终版知识库口径”以 `knowledge_base_curated.csv` 为准**。

这一步的意义不是再追求“更多条数”，而是把已经形成的 `3009` 条主知识表真正收成一份可引用、可审计、可继续同步的平台主口径。

---

## 2. 冻结前后对比

### 2.1 冻结前

在本轮处理前，主表的两项明显缺口是：

- `answer` 缺失：`40`
- `source_title` 缺失：`66`

### 2.2 冻结后

本轮已经实际回填完成：

- `answer` 缺失：`0`
- `source_title` 缺失：`0`

当前主表总量保持为：

- **`3009` 条**

---

## 3. 当前主表关键字段完整性

基于 `knowledge_base_curated.csv` 的最新审计结果：

| 字段 | 缺失数 |
|---|---:|
| `id` | 0 |
| `title` | 0 |
| `category` | 0 |
| `question` | 0 |
| `answer` | 0 |
| `source_type` | 0 |
| `source_title` | 0 |
| `source_org` | 0 |
| `source_url` | 0 |
| `tags` | 0 |

说明：

- `subcategory` 仍有 `882` 条为空；
- 这不属于本轮“关键缺失字段”范围，因为它不影响当前问答链路和主引用口径；
- 后续如要继续做分类精修，可作为单独的数据治理轮次处理。

---

## 4. 本轮处理方法

### 4.1 `answer` 回填规则

对原来缺失 `answer` 的 `40` 条 `KB-GAP-*` 条目：

- 直接用现有 `question` 中已经写好的结构化安全说明回填；
- 这样做的原因是这些条目本身已有完整人工整理内容，缺的只是字段落位，不是知识本体缺失。

### 4.2 `source_title` 回填规则

对原来缺失 `source_title` 的 `66` 条 `KB-EQUIP2-* / KB-CHEM2-*` 条目：

- 按 `source_url + source_org + title` 批量生成规范来源标题；
- 例如：
  - `Stanford Autoclave Safety`
  - `Stanford Centrifuge Safety`
  - `Cornell Laboratory Safety Manual Ch.16.6 Heat and Heating Devices`
  - `NIOSH Pocket Guide to Chemical Hazards - <化学品名>`
  - `OSHA Chemical Sampling Information - <化学品名>`

---

## 5. 当前“终版知识库口径”定义

从 `2026-05-22` 这次冻结开始，项目中的知识库主口径定义为：

> **以仓库根目录下的 `knowledge_base_curated.csv` 作为本项目当前终版知识库主表。**

含义：

1. 条目总数口径：以该文件统计为准；
2. 字段完整性口径：以该文件审计结果为准；
3. 后续评测、归档、导出和平台同步，都应以该文件为源头，而不是再混用历史中间导出物。

---

## 6. 与 Dify 正式集的关系

当前已知事实：

- 本地主表：`3009` 条
- Dify 正式主数据集：`3009` 条

本轮处理主要是**补齐本地主表字段完整性**，因此可以明确说：

> **当前“终版知识库口径”已经在本地主表冻结完成。**

同时也要实话实说：

> 若要让 Dify 平台中的正式集与这次冻结后的主表逐字段完全一致，后续仍可再做一次导出同步或增量更新。

这不影响当前“终版主表口径已冻结”的结论，但影响“平台端物理副本是否已同步到本次字段修正”的严格表述。

---

## 7. 本轮相关文件

### 已更新

- `D:/newwork/lab-safe-assistant-dify-rag/knowledge_base_curated.csv`

### 本轮脚本

- `D:/newwork/lab-safe-assistant-dify-rag/scripts/fill_kb_missing_fields.py`

### 本轮说明

- `D:/newwork/lab-safe-assistant-dify-rag/docs/proposal/终版知识库冻结说明_20260522.md`

---

## 8. 结论

本轮之后，知识库主线已经从：

> **“3009 条已经够用，但字段还不够干净”**

推进到了：

> **“3009 条主表已完成关键字段补齐，并形成了明确的终版冻结口径。”**

所以现在在申报书、验收、自查和后续同步中，都可以统一使用下面这句作为当前真相：

> **本项目当前终版知识库主表为 `knowledge_base_curated.csv`，共 `3009` 条，`answer / source_title / source_org / source_url` 等关键字段已完成补齐。**
