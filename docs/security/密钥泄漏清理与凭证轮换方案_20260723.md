# 密钥泄漏清理与凭证轮换方案（2026-07-23）

> **状态更新（2026-07-23，已闭环）**：本方案第 1–4 步**已全部执行完毕并核验**——App Key 已轮换（旧 key 401 已吊销、新 key 200，仅存于 gitignored `.env.dify_rag`）；git 历史已用 `git-filter-repo` 三遍清除（删账户备份 TSV、擦旧 key、擦管理员邮箱+主机 IP），并 `push --force --all` 改写两个远程分支；核验 origin 上旧 key/账户 TSV/管理员邮箱查询全部为空，仓库 fork 数为 0。**下文的初始提交短 hash（`ddf6b61`、`dbb0406`）是清史前的历史坐标，改写后已不复存在**（其内容已被重写且不再含泄漏，映射见 `.git/filter-repo/commit-map`）；原始历史备份为 `D:/tmp/lab-safe-backup-20260723.bundle`。本文件保留为整改过程与方法留痕。
>
> 触发：2026-07-23 全量验证中，只读安全审计发现 2 处**真实凭证泄漏**已进入 git 并推送到 GitHub 远程 `github.com:leilehuimieba/lab-safe-assistant-dify-rag`。本文件起草时是**行动方案**：破坏性操作（改写历史 + force-push）与凭证轮换需人工执行/确认，起草阶段 AI 助手已完成的仅是「从当前 HEAD 移除 + 追加 gitignore + 本文件起草」这一非破坏性部分。

## 0. 影响面判断

远程托管在 GitHub。**若该仓库为 public，则以下凭证已对全网公开，必须立即轮换**；即便当前为 private，也应按已泄漏处理（协作者、fork、缓存、第三方扫描器都可能已获取）。轮换是止血的第一步，历史清理是第二步——**顺序不能反**：先轮换让泄漏值失效，再清理历史，否则清理期间旧值仍有效。

## 1. 泄漏清单（已核实）

| # | 泄漏物 | 位置 | 当前状态 |
|---|---|---|---|
| L1 | Dify 管理员账户备份：邮箱 `[REDACTED-ADMIN-ACCOUNT]`（PII）+ 口令哈希（base64 salted）+ salt | `artifacts/dify_account_backup/account_3337153688_before_20260428_170837.tsv` | ✅ 已整改：先 `git rm --cached` 从 HEAD 移除并 `.gitignore`，2026-07-23 再经 filter-repo `--invert-paths` 从**全部历史**删除；origin 上已查无 |
| L2 | Dify App API Key `app-REDACTED-ROTATED-20260723` | `.env.dify_rag`，起草时记录为加入于初始提交 `ddf6b61`（清史前坐标）、删除于 `dbb0406` | ✅ 已整改：key 已在 Dify 控制台轮换吊销，且 2026-07-23 经 filter-repo `--replace-text` 从**全部历史**擦除字面值；`git show` 旧提交已不复存在，origin 查无 |

已核实**安全**（未入库）的：DeepSeek `sk-` key、`DIFY_ADMIN_PASSWORD`、`DEMO_PASSWORD` 均只在 gitignored 的 `.env.*` / 本地文件，历史全量扫描无 `sk-` 命中。

## 2. 第一步：凭证轮换（人工，最高优先，立即）

1. **Dify 管理员口令**：登录 Dify 后台，将 `[REDACTED-ADMIN-ACCOUNT]` 账户口令改为新的强口令（L1 泄漏的是该账户的哈希+salt，可离线爆破）。若可行，一并考虑该邮箱是否需更换/加双因子。
2. **Dify App API Key**：在 Dify 应用「访问 API」设置中**重置/轮换** App API Key，使 `app-REDACTED-ROTATED-20260723` 失效。
3. 更新本地 `.env.dify_rag` 的 `DIFY_APP_API_KEY=<新值>`（该文件已 gitignore，不会入库）。重启 demo（`scripts/start_dify_rag_local.ps1`）与云主机 `labsafe.service` 使新 key 生效。
4. 轮换完成后再进入第 3 步历史清理。

## 3. 第二步：改写 git 历史清除泄漏（破坏性，需你确认后由我或你执行）

推荐 `git filter-repo`（比 `filter-branch` 快且安全）。**执行前务必**：(a) 已完成第 2 步轮换；(b) 备份整个仓库目录副本；(c) 通知所有协作者清理后需重新 clone。

```bash
# 0) 安装（一次性）：pip install git-filter-repo

# 1) 备份
cp -r lab-safe-assistant-dify-rag lab-safe-assistant-dify-rag.backup-20260723

# 2) 从全部历史中移除账户备份文件（L1）
git filter-repo --path artifacts/dify_account_backup/account_3337153688_before_20260428_170837.tsv --invert-paths

# 3) 从全部历史中清除 App Key 字面值（L2）——用 replace-text 把该字符串替换成占位
printf 'app-REDACTED-ROTATED-20260723==>app-REDACTED\n' > ../scrub-replacements.txt
git filter-repo --replace-text ../scrub-replacements.txt

# 4) 校验历史已干净
git log --all --oneline -- artifacts/dify_account_backup/           # 应为空
git rev-list --all --objects | git cat-file --batch-check | grep -i wRwHKLfNDGRk4  # 应无输出
```

> filter-repo 会移除 origin remote（防误推）。清理校验通过后重新加回并 **force-push**：
```bash
git remote add origin git@github.com:leilehuimieba/lab-safe-assistant-dify-rag.git
git push origin --force --all
git push origin --force --tags
```

替代方案：若不便装 filter-repo，可用 BFG（`bfg --delete-files account_3337153688_*.tsv` + `bfg --replace-text scrub-replacements.txt`，随后 `git reflog expire --expire=now --all && git gc --prune=now --aggressive`）。

## 4. 第三步：GitHub 侧与善后

- force-push 后，**GitHub 可能仍在旧 commit 的缓存/PR/fork 中保留泄漏**。稳妥做法：在 GitHub 仓库 Settings 联系支持或直接考虑该敏感期删除并重建仓库；至少确认无 fork。
- 因为已在第 2 步轮换，即使残留缓存也已是失效凭证。
- 所有协作者删除本地旧库、重新 clone（force-push 改写了所有历史 hash）。
- 清理后跑一遍 `quality_gate.py` + `pytest` 确认仓库功能未受影响。

## 5. AI 助手本次已完成（非破坏性部分）

- `git rm --cached` 从 HEAD 移除 L1 账户备份 TSV（本地文件保留）；`.gitignore` 新增 `artifacts/dify_account_backup/`。
- redact 了 `历史真实运行证据补充_20260723.md` 中的云主机公网 IP（改为指向 gitignored 本地配置）。
- 未执行任何历史改写或 force-push；第 2–4 步待你确认/执行。

## 6. 诚实边界

- 本文件不含任何泄漏凭证的明文值（L1 哈希/salt、L2 完整 key 均不复述），仅给定位与清理命令。
- 「已推送到 GitHub」基于 `git remote -v` 指向 GitHub 这一事实；仓库当前是 public 还是 private 未在本次核实，但轮换与清理动作在两种情形下都应执行。
- 从 HEAD 移除 ≠ 从历史移除：在完成第 3 步前，两处凭证仍可从 GitHub 历史取到，**轮换是唯一即时止血手段**。
